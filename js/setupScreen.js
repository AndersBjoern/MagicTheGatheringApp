import { navigateTo } from "./gameManager.js";

// Eksempel på opsætningslogik
document.getElementById("start-match").addEventListener("click", () => {
  alert("Spillet starter nu!");
  // Her kan du evt. loade en ny spil-side dynamisk
});
